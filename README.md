# amol-nv/user_crud

This repository is expected to contain a Go HTTP API with CRUD endpoints.

SCRUM-101 (product inventory endpoint) implementation is not possible from the provided context because the repository contents were not provided (only README.md was listed). Please provide the existing Go source files (router/server, models, persistence layer, and any existing CRUD patterns) so I can implement the inventory CRUD endpoints and tests following the established conventions.
